{
  "name": "video-automation-ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Interface web du pipeline video, embarquee dans le binaire serveur via include_str!.",
  "scripts": {
    "tokens": "node scripts/tokens-to-css.mjs",
    "build": "npm run tokens && tsc --noEmit && vite build",
    "dev": "npm run tokens && vite",
    "verifier": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
